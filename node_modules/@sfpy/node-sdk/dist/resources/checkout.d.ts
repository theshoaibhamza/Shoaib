import { CheckoutCreateData, CheckoutCreateParams, SafepayConfig } from '../types';
import { SubscriptionCreateData, SubscriptionCreateParams, SubscriptionCreateParamsWithoutToken } from '../types/checkout';
import { Authorization } from './authorization';
export declare class Checkout {
    private config;
    authorization: Authorization;
    constructor(config: SafepayConfig);
    create({ cancelUrl, orderId, redirectUrl, source, token, webhooks }: CheckoutCreateParams): CheckoutCreateData;
    createSubscription({ cancelUrl, redirectUrl, planId, reference, }: SubscriptionCreateParamsWithoutToken): Promise<SubscriptionCreateData>;
    createSubscriptionWithToken({ cancelUrl, redirectUrl, planId, authToken, reference }: SubscriptionCreateParams): SubscriptionCreateData;
}
