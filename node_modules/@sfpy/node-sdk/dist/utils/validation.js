"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateOptions = void 0;
var validateOptions = function (options) {
    if (!options.environment) {
        throw new Error('Environment is missing');
    }
    if (!validateEnvironment(options.environment)) {
        throw new Error('Environment is invalid');
    }
    if (!options.apiKey) {
        throw new Error("API key is missing for ".concat(options.environment));
    }
    if (!options.v1Secret) {
        throw new Error("v1 secret key is missing for ".concat(options.environment));
    }
    if (!options.webhookSecret) {
        throw new Error("Webhook secret is missing for ".concat(options.environment));
    }
    return true;
};
exports.validateOptions = validateOptions;
var validateEnvironment = function (environment) {
    return ['sandbox', 'production', 'development'].includes(environment);
};
